let dayNumber = prompt("Enter a day");
switch (dayNumber) {
    case 1:
        console.log("Sunday");
        break;
    // Add cases for other days as needed
    default:
        console.log("Invalid day number");
}
